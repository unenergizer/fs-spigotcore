package com.forgestorm.spigotcore;

/**
 * Any "FeatureOptional" feature that implements this Interface will
 * have their configurations automatically loaded.
 */
public interface LoadsConfig {
    void loadConfiguration();
}
